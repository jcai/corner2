<ivyconf>
	<conf defaultResolver="maxinfo" checkUpToDate="true"/>
	<property name="${ivy.default.ivy.user.dir}" value="/home/jcai/.ivy"/>
	<property name="ivy.local.default.root"
		value="${ivy.default.ivy.user.dir}/local" override="false"/>
	<property name="ivy.local.default.ivy.pattern"
		value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]"
		override="false"/>
	<property name="ivy.local.default.artifact.pattern"
		value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]"
		override="false"/>
	<resolvers>
		<filesystem name="local">
			<ivy
				pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}"/>
			<artifact
				pattern="${ivy.local.default.root}/${ivy.local.default.artifact.pattern}"/>
		</filesystem>
		<filesystem name="shared">
			<ivy
				pattern="${ivy.repo.shared}/[organisation]/[module]/ivys/ivy-[revision].xml"/>
			<artifact
				pattern="${ivy.repo.shared}/[organisation]/[module]/[type]s/[artifact]-[revision].[type]"/>
		</filesystem>
		<url name="maxinfo-ivy">
			<artifact
				pattern="http://dev.bjmaxinfo.com/ivy/[organisation]/[module]/[type]s/[artifact]-[revision].[type]"/>
		</url>
		
		<chain name="maxinfo">
			<resolver ref="local"/>
			<resolver ref="maxinfo-ivy"/>
		</chain>
	</resolvers>
</ivyconf>